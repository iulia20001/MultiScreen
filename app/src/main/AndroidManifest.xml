<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.multiscreen">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MultiScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".canari"
            android:label="@string/can">
        </activity>
        <activity android:name=".curili"
            android:label="@string/cur">
        </activity>
        <activity android:name=".philippini"
            android:label="@string/phil">
        </activity>
        <activity android:name=".maldivi"
            android:label="@string/mal">
        </activity>

    </application>

</manifest>